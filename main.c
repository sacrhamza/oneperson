#include "oneperson.c"
#include "oneperson_add_strings.c"
int main(void)
{
	oneperson("${red} %s ${_white}${red} %d ${_red}                                                            ", "hamza", 77);
	return (0);
}
